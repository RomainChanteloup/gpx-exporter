import { Routes } from '@angular/router';
import { GpxExporterComponent } from './gpx-exporter/gpx-exporter.component';

export const routes: Routes = [
    { path: '', component: GpxExporterComponent },
];
